var Sent1 = "This is the beginning of the string"; // This is the STR 1

window.alert(Sent1);                                // and mow im calling it in a alert

var Sent2 = "and this is the end of the string"; // and this is second STR

document.write(Sent2);                          // and now its calling the second STR